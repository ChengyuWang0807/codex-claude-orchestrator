# Example Workspace

This folder is intentionally small.

- `generated/` receives formal applied outputs.
- `.cco/` receives session state, ledgers, and preview artifacts.

Use the bundled sample tasks from `../tasks/`.
